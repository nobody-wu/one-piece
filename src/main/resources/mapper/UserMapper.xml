<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cornelius.mapper.UserMapper">
	<resultMap id="UserMap" type="com.cornelius.bean.User">
		<id column="id" jdbcType="INTEGER" property="id"/>
		<result column="username" jdbcType="VARCHAR" property="username"/>
		<result column="password" jdbcType="VARCHAR" property="password"/>
	</resultMap>
  <select id="selectById" resultMap="UserMap">
    select id,username,password from user where id = #{id}
  </select>
  <select id="getUser" parameterType="com.cornelius.bean.User" resultMap="UserMap">
    select id,username,password from user where username = #{username} and password = #{password}
  </select>
  <select id="saveUser" parameterType="com.cornelius.bean.User">
    insert into user(`username`,`password`) values(#{username},#{password})
  </select>
</mapper>